# requirements.txt
# ==================
# HireFlow 后端 Python 依赖包列表
# 安装方式: pip install -r requirements.txt
#
# 每个包的用途已用注释标注

# --- LangGraph 工作流框架 ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0

# --- Web 框架 ---
fastapi>=0.115.0
uvicorn>=0.32.0

# --- 数据验证 ---
pydantic>=2.0

# --- 文档处理 (PDF/DOCX 文本提取) ---
PyMuPDF>=1.24.0          # PDF 文本提取
python-docx>=1.1.0        # DOCX 文本提取

# --- 向量数据库 (三选一，MVP 用 Chroma) ---
chromadb>=0.5.0           # 轻量向量数据库，适合 MVP

# --- LLM 调用 ---
openai>=1.50.0

# --- 数据库 ORM ---
sqlalchemy>=2.0

# --- 工具 ---
python-dotenv>=1.0        # 从 .env 文件加载环境变量
