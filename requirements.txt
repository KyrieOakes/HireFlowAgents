# requirements.txt
# ==================
# HireFlow 后端 Python 依赖包列表
# 安装方式: pip install -r requirements.txt
#
# 每个包的用途已用注释标注

# --- LangGraph 工作流框架 ---
langgraph>=1.2.0                  # StateGraph、Tool Calling 子图、interrupt 与重试能力
langgraph-checkpoint-postgres>=3.1.0  # PostgresSaver: 工作流状态持久化
psycopg[binary,pool]>=3.2         # LangGraph PostgreSQL checkpoint 使用的异步 psycopg 3 驱动
langchain>=1.3.0                  # ReAct Tool Schema、消息模型与 Agent 基础组件
langchain-openai>=1.2.0           # OpenAI 兼容 LLM/Embedding 与 bind_tools
langchain-community>=0.3.0        # 社区扩展 (Document Loaders 等)
langchain-qdrant>=0.1.0           # Qdrant 向量数据库集成 (替代 chromadb)
langchain-text-splitters>=0.1.0   # 文本切分 (RecursiveCharacterTextSplitter)

# --- Web 框架 ---
fastapi>=0.115.0
uvicorn>=0.32.0

# --- 数据验证与配置 ---
pydantic>=2.0
pydantic-settings>=2.0            # Pydantic Settings: 类型安全配置管理
typing-extensions>=4.12           # TypedDict Annotated 状态类型兼容

# --- 文档处理 ---
PyMuPDF>=1.24.0                   # PDF 文本提取 (PyMuPDFLoader 后端)
python-docx>=1.1.0                # DOCX 文本提取

# --- 向量数据库 ---
qdrant-client>=1.12.0             # Qdrant 客户端 (替代 chromadb)

# --- LLM 调用 ---
openai>=1.50.0                    # OpenAI SDK (LM Studio 和 DeepSeek 都兼容)

# --- 数据库 ORM ---
sqlalchemy>=2.0
psycopg2-binary>=2.9              # PostgreSQL 驱动

# --- 工具 ---
python-dotenv>=1.0                # .env 文件加载
python-multipart>=0.0.9            # FastAPI 文件上传支持

# --- 评估与开发 ---
jupyter>=1.0                      # Jupyter Notebook (评估报告)
ipykernel>=6.0                    # Jupyter Kernel (conda 环境注册)
pytz>=2024.1                      # 时区支持 (评估报告时间戳)

# --- 测试 ---
pytest>=8.0                       # 测试框架 (44 tests)
httpx>=0.27.0                     # FastAPI TestClient 依赖 (API 集成测试)
